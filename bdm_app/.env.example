# Base URL of the Django backend. No trailing slash.
# Apps are mounted at root prefixes (/auth, /delivery, ...) — there is NO /api prefix.
# Local dev (Android emulator -> host machine): http://10.0.2.2:8000
# Local dev (iOS simulator):                    http://localhost:8000
# Production:                                   https://bdmpharmacy.store
EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:8000
